<!-- Starter default placed by `evie-kit setup` (EVA-144). This file is
     YOURS: edit it to your project's voice — setup never rewrites it.
     Prose-only BY DESIGN: no lint reads this pair, ever — mechanical
     humanizer tuning stays in the settings layers (`tools.humanizer`).
     Agents read it at every writing seam: drafting, the writing-style
     panel's editor lanes, any writing review gate.

     The addendum contract: this TRACKED half is the generic project
     voice, owned by the repo. The gitignored personal half,
     writing-style.local.md, is each writer's addendum and WINS on
     contradiction; a panel run's --rules file layers above both, for
     that run only. Overrides are prose ("where the house style says
     X, I prefer Y") — no rule ids, no mechanical suppressions:
     suppressing or demoting a lint rule happens in .evie-kit/settings.ts
     (or the settings.local layer) under tools.humanizer.rules, which
     is TypeScript — typed against the rule structure, and
     modularizable by importing the suppression block from a separate
     file. On a detected contradiction or ambiguity between layers,
     agents ask the user and write the clarification back into the
     LOCAL file.

     Everything in this scaffold sits in comments, so an unedited
     starter contributes NO voice guidance — uncomment nothing;
     replace the prompts below with your project's actual voice. -->

# Writing style — the house voice

## Register

<!-- Describe the register your project writes in — deliberate
     divergences from the humanizer's defaults included, so agents
     defend your style instead of "fixing" it. The kind of fact that
     belongs here: whether em dashes are budgeted or banned; whether
     bold lead-in bullets are house register or a tell; heading case;
     wrap width; whether first-person judgment is welcome. -->

## Purpose profiles

<!-- Name the document kinds this repo ships and who each one
     teaches — e.g. archival records (dense, provenance is content),
     agent-facing skills (precision beats brevity), human-facing
     READMEs (short sentences, paste-able commands), outbound prose
     (the gate-worthy surface). -->
