[defaults]
# run.sh always passes -i with the private generated inventory.
host_key_checking = True
retry_files_enabled = False
interpreter_python = /usr/bin/python3
gathering = explicit
nocows = True

[ssh_connection]
pipelining = True
