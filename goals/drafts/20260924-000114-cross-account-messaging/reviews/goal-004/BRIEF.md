---
round: 4
timestamp: 2026-09-24T06:41:12.610Z
author: main-agent
---

# Round brief — goal-004

Main-agent brief to the goal critics of `20260924-000114-cross-account-messaging` — the
outbound half of the file-mediated loop, committed beside the critiques
so both directions are auditable. Engines this round: claude-code, codex, pi-gemini.

## Update

# Verify final dependency-coverage and observation corrections

Goal-003: Opus ready, Pi/Gemini advisory ready, Astra not_ready on one precise
omission: the required OTel archive's dependency lock records. Fixed by including
candidate packages/otel/package-lock.json and requiring complete production/peer
coverage with version/resolved/integrity, with missing/conflicting entries blocking.

Also folded Opus's nonblocking notes into the existing contract: T5 observation
between original and hypothetical publish-refreshed expiry deadlines, invalid
window cannot pass; Redis default off at startup before listen and data/config/
admin secrets controller-only readable with per-UID denials; installed public
config loader for library cases, mapped inputs for raw ACL probes.

No other scope or owner choice changes. No OTel behavioral suite added. Pi's
requirement for two installer caches is rejected: both runtimes execute the same
installed consumer tree, and only the chosen installer's verified cache is needed.

Verify this exact correction batch and confirm promote readiness on the current
files. Avoid expanding the engineering checklist with unrelated optional detail.
All exact bootstrap scripts, path names and resource limits remain executor
checkpoint deliverables. All 200 cases and the narrow authority boundary remain
unchanged. Do not run product tests or provision anything. Preserve normal
critique structure and write only your goal-004 critique. Pi remains advisory.

## Instructions to critics

Re-read the goal spec (GOAL.md) and this brief, then write THIS round's
critique to `<your-engine>/CRITIQUE.md` inside this round
folder, following the full contract in `<your-engine>/PROMPT.md`
(required sections, provenance frontmatter, promote-readiness verdict,
completion marker).
